# Secrets — NEVER commit
config.py

# Python
.venv/
__pycache__/
*.pyc

# Local data
*.db
chat.db

# Personal data — the Anthropic export is your whole history, keep it local
claude_export/

# Editor / OS cruft
.vscode/
.DS_Store
# Windows "downloaded from the internet" alternate-data-stream files
*:Zone.Identifier

# Claude Code local files
.claude/
# Personal working notes — has names, filepaths, private context.
# CLAUDE.example.md is the sanitised version that ships.
CLAUDE.md

# golden harness scratch
tests/_fixture.db

# personal idea capture, not project history
WISHLIST.md

# roadmap planning detail for versions not yet shipped — see ROADMAP.md
ROADMAP_PRIVATE.md

# grouped, ordered ideas beyond v1.0, no version numbers yet — see ROADMAP.md
ROADMAP_BEYOND.md
