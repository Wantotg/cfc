[pytest]
testpaths = tests
# auto: only tests/test_cfc_tui.py's Pilot-driven tests are async def; every
# other 2.0 async test (send_turn etc.) deliberately uses asyncio.run()
# directly per this loop's Work Order, so this setting affects that one file.
asyncio_mode = auto
