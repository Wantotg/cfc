# Closed tracker records

This manager-maintained archive holds the closed rows removed from
`workspace/TRACKER.md`. Permanent ids are never reallocated. The table keeps
the closed finding and its reason so a later playtest does not reopen it as if
it were new.

`workspace/TRACKER.md` was reduced to open work on 2026-08-08. The rows
below retain their existing shape and wording; the two rows added at the top
are the closed records reconciled in that pass.

## Closed

| id | what | why it closed | when |
|---|---|---|---|
| D-22 | five test files set the shared console and never restore it at all | deliberately closed at the 2.0 entry gate: the old scripts are not repaired solely for the abandoned implementation. Any retained or rewritten 2.0 proof must isolate and restore shared output state; the exact Rich-console mechanism is not carried forward | 2026-08-08 |
| N-1.6.4-09 | the ID column still shrinks with every other column on a terminal too narrow for the table | already closed in the current tracker; retained here as a historical closed record | 2026-08-08 |
| D-24 | the `W-1.1-02` `CHANGELOG.md` entry had no heading and read as part of `D-1.6.2-02`'s | corrected in the v1.9.1 manager closeout; the 5xx entry has its own heading again | 2026-08-08 |
| D-23 | `parse.py` and `main.py` still told a reader an unrecognised verb reaches the model | corrected in the v1.9.1 manager closeout, including standing decision 13: a slash-addressed line now dispatches or visibly refuses | 2026-08-08 |
| D-21 | an unreadable outbox root made the proposal screen imply the outbox was empty | shipped in v1.9.1, `ad95082`; an incomplete count names the unreadable root condition and the closed body left `BACKLOG.md` | 2026-08-08 |
| W-1.6.3-01b | the tone check fired on every ordinary turn and could not be turned off | shipped in v1.9.1, `05044bf`; `GOVERNOR_TONE_CHECK` is a default-on deployment choice | 2026-08-08 |
| D-1.6.2-02 | an untracked vault file could not be inspected before it was committed | shipped in v1.9.1, `c9091d6`; `/wiki diff` can preview one untracked file read-only | 2026-08-08 |
| W-1.4.1-02 | an unrecognised `/command` reached the model instead of being refused | shipped in v1.9.1, `cddc7af`; the slash namespace now visibly refuses an unknown or empty command | 2026-08-08 |
| W-1.1-02 | a raw provider body was not an error a human could act on | shipped in v1.9.1, `8df6924`; cfc owns 5xx wording while preserving 400 detail | 2026-08-08 |
| W-08 | cfc could not tell a config typo from a real model id | shipped in v1.9.1, `f66cc76`; suspicious multi-slash ids are marked, never refused | 2026-08-08 |
| D-19 | twelve test suites pin the shared console, and later captures can silently read empty | shipped in v1.9.1, `f05602f`, and completed in the triage, `8614d32` — the one tag-blocking finding, and not from the playtest. The first pass saved `console.file` and restored that object; Rich's `file` getter cannot report the unset state, so on a console holding no file of its own it answers with the live `sys.stdout` and the restore pins the console to a terminal handle — the leak the row exists to close, wearing the fix's clothes. Live: `python -m pytest tests/` failed `test_empty_retry.py::test_both_empty_exits_announce`, which read an empty buffer while its line went to the screen, pinned there by `test_connection.py`'s helper — one of the four the Concept had named already correct. Fails identically at `453eb85`, so v1.9.1 did not cause it and did not close it. All 59 save sites now read `console._file`; `tests/test_ui.py` gains the unset case beside the nested one. Verified by disabling. `CHANGELOG.md` | 2026-08-08 |
| N-09 | the coder handoff's proof line `python -m pytest tests/ -q: 58 passed, 1 deselected` does not reproduce | nothing owed in code, and the observation is the useful half. The repo has no `pytest.ini`, `setup.cfg`, `pyproject.toml` or `conftest.py`, so nothing deselects anything — the run must have been given `--deselect` by hand, and the stated command produces `1 failed, 58 passed`. The failure was then explained in `Update.md` as pre-existing and out of scope, which it was, and the red suite reached the playtest as a green claim. It is `D-19`'s own claim that it falsified. Rule rather than fix: a proof line quotes the command that was actually run | 2026-08-08 |
| N-08 | `wikigit.diff_untracked_file` revalidates existence, type and symlink scope, but not that the path is still an untracked `Change` | nothing owed. `Concept.md` asked for the path to be "a current untracked `Change` inside the chosen repo scope"; what ships checks the half that can be violated. The path reaches the function only from `_pick_change`'s own list, which `commands._wiki_diff_file` built from `status(scope)`, and a plain relative path cannot resolve outside the scope it was listed under — the one way out is a symlink, and that is checked against `scope_dir(scope) or repo_root()`. The residue is a file that became tracked between the listing and the pick: `--no-index` would render it as a whole-file addition rather than its real diff. Read-only either way, no index touched, no authority widened, and a rebuilt picker is one keystroke away | 2026-08-08 |
| N-07 | a chat line that begins with `/` and isn't a command is now refused instead of sent | nothing owed — this is `W-1.4.1-02` as designed, and the cost is worth naming once. `parse` splits on the first space, so `/home/disse/notes.md` is the verb `home/disse/notes.md` and draws `Unknown command`. Every line addressed to the `/` namespace is dispatched or visibly refused, which is the whole claim; the refusal is dim, immediate, costs no API call and loses nothing typed. Recorded so a line-initial path doesn't come back next playtest as a regression | 2026-08-08 |
| D-20 | the v1.9 loop-2 `CHANGELOG.md` entry still said `Commit: pending` | backfilled `ef2f3ad` in this manager pass, the next commit after the entry was created | 2026-08-07 |
| D-1.7-02b | `/file`, `/move` and `/list outbox` described the same folder with different scopes | fixed in `ef2f3ad`; the three surfaces now name their subsets and the body moved whole to `legacy/BACKLOG.md` | 2026-08-07 |
| W-1.7-02c | nothing in cfc showed what was in the outbox | shipped in `ef2f3ad`; `/list outbox contents` is a bounded read-only inventory | 2026-08-07 |
| W-1.9-01c | a wiki session never named the vault file behind it | shipped in `ef2f3ad`; open and `/status` resolve the current source from frontmatter id | 2026-08-07 |
| W-1.7-01c | whether routine transcripts with human conversation belong in Recent chats | closed by decision in `ef2f3ad`; Recent chats remains provider-kind based, so routine transcripts stay out | 2026-08-07 |
| D-1.9-04 | the v1.9 `CHANGELOG.md` entry still said `Commit: pending` | backfilled `2fc9914` in this manager pass, the next commit after the entry was created | 2026-08-06 |
| N-1.9-03 | an empty chat survived the playtest | nothing owed; the process closed without reaching the controlled-departure cleanup, and the same run discarded three other chats | 2026-08-06 |
| Q-1.9-02 | how cfc treats wiki pages, and how fragile that model is | answered in `Tag.md`; the frontmatter-id deletion/copy sharp edge is already `B-11` | 2026-08-06 |
| N-1.9-01b | two wiki sessions have the same title and different message counts | nothing owed; two vault files with distinct frontmatter ids are two pages by standing decision 9, and provider failures explain the message counts | 2026-08-06 |
| N-1.9-01a | a wiki page continues as a chat when opened | nothing owed; `W-1.6.4-05` shipped the behaviour and its opening banner states it | 2026-08-06 |
| W-1.5.1-02 | the hub's `c` prompt doesn't say how to leave, and a non-number reads as an error | fixed in `2fc9914`; Enter cancels and non-numbers stay on the id path | 2026-08-06 |
| D-1.7-01d | an abandoned empty chat holds a picker row forever, `Latest message` set with no message | fixed in `2fc9914`; untouched automatic chats are discarded on controlled departure | 2026-08-06 |
| D-1.7-01b | the hub's `<number>` help line says "the ids in the table above", but any session id opens | fixed in `2fc9914`; numeric help now covers non-Main ids and `/list sessions` | 2026-08-06 |
| W-1.8-02 | a refused search names a code but never the HTTP status, and never says the query certainly arrived | fixed in `2fc9914`; `source_refused` carries the status and definite receipt wording | 2026-08-06 |
| D-1.8-03 | the v1.8 `CHANGELOG.md` entry still said `Commit: pending` | backfilled `679828c` in the manager closeout. The entry was created by that commit, and this pass was the next commit where its final hash could be recorded | 2026-08-04 |
| N-1.8-01 | `web_search failed — source_refused` on the sixth live search in a row | nothing owed, and the report's premise is right. `source_refused` is written at one place (`search_worker.py:253`) and is reachable only after curl completed an HTTP exchange, so the line proves DuckDuckGo received the query and refused it with a status code — `Concept.md`'s own failure table names that case with the example statuses `202, 403, 429`. Reproduced outside cfc with the worker's own curl invocation: four consecutive 200s with real results, then HTTP 202 with a 14 KB `anomaly.js` bot-check page from the fifth on. No `Retry-After`, and it does not clear quickly — still 202 after ~3 minutes idle, and still 202 after a further 13 minutes with no traffic at all, so read the cooldown as *at least* a quarter of an hour (a refused request may itself restart the window; that is not separable without more knocking). Not a fixed count — Cas's fifth got through and mine didn't. The 202 is the favourable half: the same page served as 200 would degrade to `parse/markup_unrecognized`, which `Concept.md` deliberately accepts rather than sniff for "captcha"/"verify". Every available remedy — solving the challenge, a browser user-agent, a retry, a second engine — is in `Concept.md`'s *This is not* list. The half with work in it is `W-1.8-02`: the status number itself is not reported. Practical ceiling, worth knowing rather than building around: roughly four searches per burst, then live search is gone for the rest of the sitting. `Tag.md` | 2026-08-04 |
| Q-1.7-04b | what does "rename `bwrap` off your `PATH`" mean, and should `websearch.py` 46–48 have been edited to test it? | answered in v1.7's `Tag.md`. `PATH` is the colon-separated list of folders the shell searches for a command name; `bwrap` is `/usr/bin/bwrap`, and cfc asks the same question with `shutil.which`. It is not a file in the project, which is why VS Code can't find it. `websearch.py` 46–48 are cfc's own worker/protocol paths and are unrelated — editing them produces a different `sandbox_unavailable`. The path is already covered: `tests/test_websearch.py` points `websearch.BWRAP_BINARY` at a name that isn't there and asserts `search()` fails closed with no unsandboxed fallback, verified by disabling the guard. Same one-liner reproduces the `/tools` string by hand | 2026-08-04 |
| N-1.7-03b | the v1.7 report's `01` and `02` collide with `B-1.7-01` and `D-1.7-02`, allocated by the earlier 1.7-numbered pass and closed in v1.6.2 | nothing owed. Ids are permanent and in pushed commit messages, so this pass takes letter suffixes — the shape `B-1.6.4-09b` and `B-1.6.3-01a` already use. Different in kind from v1.6.4's collision, which was a second loop continuing into its own number space: this is v1.7's **first** playtest arriving at a space an earlier report had already spent on findings fixed in v1.6.1/v1.6.2. Prevention offered rather than imposed: the report template's *every item gets an id* note could add "check `TRACKER.md` for the highest already allocated". Cas's file, Cas's call | 2026-08-04 |
| B-1.6.4-09b | "Enter to confirm, or 'back'" confirmed on anything typed — `/clear notes` archived on `apifjaf`, and `/move`'s ordinary confirmation had the same hole | fixed in the v1.6.4 round-2 triage, `362d4e1`. Both sites compared the line to `back` and fell through to the action otherwise; the wording existed three times and only `/move`'s rename confirmation enforced it. `commands.confirm_or_back` is the one reader now — Enter acts, `back` cancels, anything else says `not recognised` and asks again — so the printed contract and the rule are the same object. Recoverable in both cases, but `/clear notes` exists so a human declares the batch closed after the routines have read the inbox, and an accidental clear takes it from a routine that hadn't. The `input()` shell above `notes.py`/`mover.py` had no test at all; `tests/test_notes.py` now drives `_do_clear_notes` with a scripted input across all four cases and fails if a fourth copy of the wording appears, verified by restoring the old behaviour. `CHANGELOG.md` | 2026-08-03 |
| N-1.6.4-08b | `/wiki diff vault` printed `\ No newline at end of file` twice around a `+---` | nothing owed, and the file is Cas's own. That marker is `git diff`'s, not cfc's: it appears when a side of the hunk has no final newline, and both sides here did not. The only tracked modified file in the vault at the time was `00 inbox/v.1.6.4 round 2 playtest.md` — the round-2 report itself, written in place over the round-1 one by Obsidian, last byte `-`. Verified against the real vault repo: `git diff` on that path reproduces the exact three lines. cfc renders what `git diff` produces and must keep doing so — suppressing the marker would mean printing a diff that does not match the repository's. Two of 45 tracked files in the wiki and journal corpora also lack a final newline (both declined drafts in the losers' corner), because `tools.write_file` writes model content verbatim and `mover` preserves it; that is content fidelity, not a defect, and it is a rare and cosmetic diff-noise cost | 2026-08-03 |
| D-18 | the only advice on setting a missing context limit is in `show_token_stats`, which nothing calls | already satisfied by `D-1.7-02` and `B-10`; the context-limit wording and hub legend shipped in v1.6.2, so no separate work remains | 2026-08-03 |
| D-1.6.4-08 | the test suite writes fabricated exports into the real `CHAT_EXPORT_DIR` | `run_session` now takes a required keyword-only `auto_export`, no default; `repl()` is the one production caller and always supplies its configured `AUTO_EXPORT`. Every direct test caller now states its own choice explicitly instead of inheriting whatever config happened to hold. `CHANGELOG.md` | 2026-08-03 |
| W-1.6.4-02 | `finishing turn` announces work starting and nothing ever says it stopped | replaced with a Rich transient status naming exactly the jobs about to run (title, embed, both, or neither), decided before either starts and cleared before the next prompt. `CHANGELOG.md` | 2026-08-03 |
| W-1.6.4-05 | a wiki page and a routine transcript can be listed but never opened | `resolve_open_target` now refuses only a missing id and Main; `run_session` derives Main/wiki/routine/chat itself off the row's `provider`, no caller-supplied flag any more. Settled wiki notice on open, `/list sessions`' Kind column, provider-based ordinary-chat-only titling, `/swipe`/`/undo` refusal on wiki/routine, and message-level wiki source identity in `chunk_new`/`import_wiki` all landed with it. `CHANGELOG.md` | 2026-08-03 |
| W-1.6.4-04 | nothing shows a human what cfc actually sent on a turn | `run_session` keeps a process-local `request_capture`, populated by `api.call_api`/`stream_response` via the `capture_requests` context manager (never a parameter, so no existing caller or test stub needed to change) and rendered by `/status request` as `Call N of M`. `CHANGELOG.md` | 2026-08-03 |
| Q-1.6-02 | a chosen high chat id becomes the floor for every auto-assigned one after it | corrected in the v1.6.4 triage, `36e6c69`; chosen ids no longer advance the automatic mark, and allocation steps over occupied ids. The original `3a6fc18` rule was inverted; the live mark was repaired after backup and the wide-id sessions were retained because routine logs reference them | 2026-08-03 |
| W-10 | rename a chat from the hub — `/title <id> <name>` works, but only from inside a chat | shipped in `3129e45`; triage commit `f4ba189` corrected the result wording when the renamed chat is outside the ten displayed rows | 2026-08-03 |
| B-1.6.3-01a | the tone direction is answered as if you had typed it | fixed in v1.6.4, `8444ea5`, and **measured against the provider that reported it** in the triage rather than inferred from the playtest behaving. Session 1982471289471209's real `ok` envelope was rebuilt from the live database through the real `governor`/`assemble`/`api.wire_messages` path and sent to `google/gemma-4-31b-it` under both tone texts, everything else byte-identical: v1.6.3's reproduces the bug 3/3 (*"Understood. I have noted the direction regarding tone adjustments…"*), v1.6.4's answers the user 3/3, one reply byte-identical to what the playtest got. The playtest alone could not close this — a model behaving and a direction never arriving produce the same screen, which is what `W-1.6.4-04` is. `CHANGELOG.md` | 2026-08-03 |
| B-1.6.4-01 | a chosen chat id became a permanent floor for every automatic one, and the hub could only open the ten rows it printed | fixed in the v1.6.4 triage, `36e6c69`/`1b1ef63`. Two faults under one report. v1.6.4 let `create_chat` raise the durable mark, so a sixteen-digit id typed at `c` pushed it to 1982471289471204 and `d` — which `N-1.5-01` recorded as the whole undo for exactly that mistake — deleted the row without lowering it; every session after carried a sixteen-digit id, permanently. `create_chat` now leaves the mark alone and `alloc_session_id` steps over an occupied id, verified by disabling the skip. Separately the picker matched typed ids against the ten rendered rows, so a chat older than those could not be opened at all while `r` and `d` resolved any id; `db.resolve_open_target` is the one check now, keeping the wiki/routine refusal the row check was really protecting. Live database repaired by hand after a verified backup: mark 1982471289471218 → 955, the fourteen wide-id sessions left in place because four are routine transcripts whose ids the run log carries. `CHANGELOG.md` | 2026-08-03 |
| B-1.6.4-07 | a session table's ID column was fixed at width 4, so any id of five digits or more rendered truncated | fixed in the v1.6.4 triage, `e24b320` — `_id_width` measures the rows and `_session_table` takes them, ID leaving `_CHROME` so the flexible three divide what is left. Landed in the picker, whose whole contract is that you type back the number it printed, and in `/list sessions`, where `_add_rows`' own comment explains why one number everywhere exists. Pinned by rendering through the real table and reading the painted output, verified by putting `width=4` back. The residual — every column shrinking on a terminal too narrow for the table — is `N-1.6.4-09` | 2026-08-03 |
| W-1.6.4-06 | a rename reported `Session #N titled: X` and the redraw didn't show the renamed chat | fixed in the v1.6.4 triage, `f4ba189` — the result names the verb typed and both titles, and `_hub_rename` says when the chat is outside the ten rows the redraw shows. `Concept.md` claimed the redraw "immediately shows the changed title" *and* that this flow may rename a chat outside those rows; for such a chat both cannot hold. Not fixed by bumping `updated_at`, which is what the "Latest message" column describes. `/title` shares the wording through `rename_chat`; golden re-recorded on that one line | 2026-08-03 |
| N-1.6.4-05a | the hub's `r` refused to rename wiki session #1 | nothing owed on the refusal — `Concept.md` and the work order both ask for wiki sessions, routine transcripts, Main and a missing id to be visibly refused, and the message named which of those #1 was. The half of the report with work in it is *then let me open it*: a wiki page and a routine transcript are listed by `/list sessions` and openable from nowhere, which is `W-1.6.4-05` | 2026-08-03 |
| N-1.6.4-03a | `/swipe` refused after three `/continue`s | nothing owed on the refusal being *correct* — `classify_latest_turn` anchors on the last user row and counts the answers after it, so three continuations read as four answers to one send and it declines rather than guess which to remove. The premise the report is right about is that it should not have to: `/swipe` rerolls the last AI message, and `/continue` is the other side of the conversation. Why it cannot today is concrete — rows 3309 (a chat answer) and 3310 (a `/continue` answer) in session 1982471289471209 are byte-identically shaped, `meta` NULL on both, so nothing records which action produced an assistant row and a reroll cannot know which turn to re-send. That is `W-1.6.4-03`, Cas's call to take after 2.0 | 2026-08-03 |
| N-1.6.4-10 | `test_hub.py` restored `console.file` to `sys.stdout` instead of None | corrected in `f4ba189` while adding coverage next to it, not reported by the playtest. Rich resolves its file dynamically only while it holds none of its own, so assigning `sys.stdout` pinned it and every later `redirect_stdout` in that file captured nothing while still looking like it worked — a test that passes on an empty string. Found by writing an assertion against captured output and watching it fail on content that was demonstrably printed | 2026-08-03 |
| N-1.6.3-02 | `/update db` named seven wiki files with no id, not the two the check expected | nothing owed, premise wrong. Seven top-level `.md` files in the wiki directory genuinely have no frontmatter id — all seven created 12:36 on 03-08, byte-identical keyboard-mash, `,,,,,....md` and six Obsidian duplicates of it. The warning named all seven, sorted, relative to the wiki directory: `B-1.6.2-01a` working as specified. The expected "two" came from the work order's checklist, which says two because the *test* uses two. Not cfc's doing either, checked rather than assumed — `mover.py`'s collision walk appends `-<timestamp>`, never ` 1`/` 2`/` 1 1` | 2026-08-03 |
| N-1.6.3-03 | the missing-id warning names every skipped file with no ceiling on how many | nothing owed. Raised by the debugger while reading, not by the playtest. Seven names already wrap to two lines and two hundred would be a wall — but the importer reads one directory's top-level `*.md` and nothing else, `Concept.md` asked for every name on purpose (not repairing one file and rerunning to find the next was the whole finding), and a wiki with two hundred id-less top-level pages is a vault problem the warning would be correctly reporting. A cap reintroduces the bug the feature was built to fix | 2026-08-03 |
| B-1.6.2-01a | `/update db` says a wiki file has no id and won't say which | fixed in v1.6.3, `64d995a`: the importer carries every skipped filename through to the existing partial-import warning, and the focused test covers the names and relative paths; `CHANGELOG.md` | 2026-08-03 |
| D-17 | `errorlog` records the turn's *module*, never its kind, so "is `/swipe` luckier than a resend" can't be counted | fixed in v1.6.3, `e391ef7`: the four chat-turn actions now write `turn <kind>` while title and routine failures keep their existing origins; `CHANGELOG.md` | 2026-08-03 |
| D-14 | `ui.vault_relative`'s docstring names `VAULT_PATH`; its one caller passes `VAULT_ROOT` | fixed in v1.6.3, `df2995c`: the docstring names the key the caller actually passes; `CHANGELOG.md` | 2026-08-03 |
| B-1.7-05 | a wiki draft's empty frontmatter `id:` produced two keys and prevented indexing | fixed in v1.6.2, `0f3c122`: filing removes the empty key before writing the generated id, and the filing/import boundary is covered; body moved whole to `legacy/BUGS.md`, `CHANGELOG.md` | 2026-08-03 |
| B-1.7-01 | whitespace-only reasoning drew an empty panel | fixed in v1.6.2, `0f3c122`: the streaming path applies the readable-content guard while retaining raw reasoning; body moved whole to `legacy/BUGS.md`, `CHANGELOG.md` | 2026-08-03 |
| D-1.7-02 | unknown model limits made context usage disappear between displays | fixed in v1.6.2, `0f3c122`: chat, `/status`, post-turn and hub displays state the unknown limit; body moved whole to `legacy/BACKLOG.md`, `CHANGELOG.md` | 2026-08-03 |
| D-1.7-04 | `/move` and `/outbox` described overlapping files as different things | fixed in v1.6.2, `0f3c122`: both screens name their lists and explain the overlap; body moved whole to `legacy/BACKLOG.md`, `CHANGELOG.md` | 2026-08-03 |
| B-10 | the hub's `Ctx` column renders `N / ?` and the `h` screen never says what it means | fixed in the v1.6.2 triage, `86a089e` — the one tag-blocking finding, and found by reading `Concept.md` against the code rather than by the playtest: step 3 asks for the cell *and* for the hub to explain it, and only the cell shipped. `print_hub_help` now renders its example cells through `_context_cell` itself, the way the light's legend is rendered by `connection_light`, so the pair is closed by construction rather than added to the hazard table; both examples use an unknown model id, which has no configured limit, so nothing from `MODELS` reaches the screen or a baseline (`B-1.6-05`'s scar). `tests/test_hub.py` round-trips it against the renderer, verified by printing a literal in the help and changing the cell's format. `CHANGELOG.md` | 2026-08-03 |
| N-06 | `/move`'s empty state says "No outbox files are available to move", dropping the "top-level" that `D-1.7-04` exists to teach | nothing owed. `Concept.md` proposed "No top-level outbox files to move."; `Work Order.md` relaxed it and the shipped string matches the order. The listing header and `/help` both carry the new vocabulary, and the empty state is the one case with nothing to categorise, so the missing adjective teaches nobody anything wrong. Recorded so it doesn't return next playtest as a missed rename | 2026-08-03 |
| D-1.6-06 | two `CHANGELOG.md` entries still say `Commit: pending` (v1.2, v1.2.1 era) | corrected by backfilling v1.2 with `1a91f21` and v1.2.1 with `4a522ac` in `legacy/CHANGELOG-post-1.0.md`; no open work remains | 2026-08-03 |
| B-1.6-01 | `diff <anything>` silently diffs the wiki instead of refusing; `status` ignores its arguments too | shipped in v1.6.1, `2c4df49` — `commands._wiki_diff_accept` / `_wiki_status_accept` are the one acceptance decision both readers call, before any git call, and `screens._wiki_diff` arms its review off `show_wiki_diff`'s returned scope instead of parsing the argument a second time. Driven live at a real REPL in this triage, chat verb and wiki screen, which is the half `tests/test_screens.py` could not claim. `commit` untouched. Body moved whole to `legacy/BUGS.md`; `CHANGELOG.md` | 2026-08-03 |
| D-1.6-03 | `/update db`'s hidden-wiki notice doesn't say the chat index still runs | shipped in v1.6.1, `2c4df49` — one line, before the spinner, naming both halves. The sibling defect on the *other* branch (a page skipped for a missing id, followed by a spinner that looks like it contradicts the warning) is `B-1.7-05` and is a different fault, not this one recurring. Body moved whole to `legacy/BACKLOG.md`; `CHANGELOG.md` | 2026-08-03 |
| Q-1.7-06 | the coder handover's "which claims are now testable" is unreadable to the person it is written for | answered in v1.6.1's `Tag.md`, in plain words, claim by claim. The finding underneath is real and is a rule rather than a fix: that section is the one part of a coder handover written **for Cas**, so it says what to type and what should happen, and names a test file only after that. A claim stated in the vocabulary of the diff proves nothing to the only person who can falsify it | 2026-08-03 |
| N-1.7-07 | `_parse_wiki_args` splits and rejoins, so a commit message typed with a double space reaches `wikigit.commit` with one | nothing owed. Raised by the coder, not the playtest. The tokenizer is how the optional scope and granularity words are consumed at all, the collapse is invisible in a git log, and no cfc feature parses a commit message back. The one-line fix (slice the original string at the consumed prefix) is available if a message ever needs exact whitespace — a code block in a commit message would be the case, and nothing does that today | 2026-08-03 |
| B-1.6-05 | v1.6's two new `/config` rows made the golden baseline and a new `test_pools` case pin `config.py` again | fixed in `20d4e14`, v1.6 triage — the only tag-blocking finding, and not from the playtest: the suite went red on Cas's machine the moment he declared his own `VAULT_SCOPES` and display names. `capture()` now pins `VAULT_SCOPES` empty and both names absent, beside the `VAULT_ROOT`/`AUTO_EXPORT` pins that exist for the same reason; scopes are pinned *empty* because `capture()` repoints `VAULT_ROOT` at the fixture vault, so a real declaration resolves to directories that don't exist there. Both fixes restore the recorded baseline untouched, which is the evidence they were leaks and not intended changes. `CHANGELOG.md` | 2026-08-02 |
| N-1.6-04 | an exposed vault scope did not make its folder writable — a `write_file` into the journal was auto-denied | nothing owed, and the report's premise crosses two boundaries. A scope answers *may a model-facing surface reach this path*; `WRITE_ROOTS` answers *may anything be written here*, and standing decision 4 has it at the vault outbox and nothing else, never derived from the read side. The refusal named the allowed root, so the mechanism reported itself correctly. What the scopes screen doesn't say is that `exposed` means readable — the vocabulary gap, and the design question of whether a scope may ever declare a write root, is `W-1.6-04` | 2026-08-02 |
| D-16 | `runner._mark_transcript` swallows a failed marker write without rolling the connection back | shipped in v1.6, `d73b7ec` — the rollback runs in the `except` before the failure is swallowed, verified by disabling it and watching a stray partial INSERT ride along on the next save and survive. Body moved whole to `legacy/BACKLOG.md`; `CHANGELOG.md` | 2026-08-02 |
| B-1.5.1-01a | a `--run-due` tick dies with `database is locked` while a chat is open; the routine doesn't run and nothing says so | shipped in v1.5.2, `eb8db69`/`4e747bb`, and **confirmed against the live database with a real second process**: the pre-fix `db.py` opens `~/.cfc/chat.db` with `in_transaction` True under no contention at all, and dies `database is locked` after 5.02s against a held write lock — the exact traceback the 10:45 and 11:00 ticks logged. The shipped `db.py` opens through the same held lock in 0.00s with `in_transaction` False, because it now takes no lock to release. `schedule.DB_OPEN_TIMEOUT = 30.0` was never reached in either run and is insurance, not the fix. `CHANGELOG.md` | 2026-08-02 |
| B-1.5.1-01b | a routine that did its work left no line in the run log, because the failure handler writes the transcript first | shipped in v1.5.2, `076c806`/`4e747bb` — one outcome boundary around session creation, task persistence, the tool turn and final persistence; `append_log` first and the transcript marker after, best-effort; success commits before `ok`. Pinned by injected failure at all six points (`tests/test_routines.py::test_run_log_authoritative`). The original field symptom needed a real lock to bite and was not re-driven; the injected exception is the same exception, and the lock that produced it is now removed by `B-1.5.1-01a` above. `CHANGELOG.md` | 2026-08-02 |
| D-1.5.1-01c | the routines screen can't say which routine is due — the config screen's attention count points at it | shipped in v1.5.2, `49e8df2` — one captured clock per render feeding `schedule.assess(routine, now).state` into a `Schedule` column (wide) and a `schedule` line (narrow); `show <routine>` keeps the full `reason`. Driven in the playtest by editing real triggers around the current time and cross-reading hub against screen; both agreed. Tracker had it at v1.6; the v1.5.2 scope adopted it and this row is the reconciliation. `CHANGELOG.md` | 2026-08-02 |
| B-09 | the NULL-kind backfill ran its `UPDATE` and never committed it, so `db()` returned holding an open write transaction for the connection's whole life | found by reading in the v1.5.2 debug session and fixed the same day, `f93b341` — the commit was gated on `added or wrote_marker`, which didn't cover the backfill; one `wrote` flag now covers all three writes. This is `B-1.5.1-01a`'s retained writer surviving inside its own fix, on the one fixture the fix's own test named. The test passed because it read the value back on the connection that wrote it, which sees its own uncommitted transaction — the new assertions check `conn.in_transaction` and re-read after a reconnect. Unreachable on any live database (nothing inserts an explicit NULL `kind`; the column's `DEFAULT 'chat'` covers every omitted-column insert), which is why the playtest could not have found it. `CHANGELOG.md` | 2026-08-02 |
| Q-1.5.2-01 | is there a technical reason `/swipe` 503s less often than sending a second message? | answered — yes, one, and it is small. cfc retries nothing on a chat 503 (only routines re-roll, `runner._turn_with_retry`), so both actions are one fresh request. But the failed send already saved its user row *before* the request, so `/swipe` prunes zero answer rows, keeps that row, and re-sends a **byte-identical** request (`governor.ordinary_instruction` is deterministic and the kept row holds `turn_count` still); a typed second message sends a strictly longer one ending in two consecutive `user` turns. Whether nano-gpt's router treats those differently is not knowable from here, and the two mechanisms that *are* knowable both point the other way: composing a new message takes longer than typing `/swipe`, which should favour the resend if outages are bursty, and prefix-sticky routing would pin an identical prompt back to the same edge. `D-17` makes it countable | 2026-08-02 |
| W-0.9.2-01 | a run frozen by a machine suspend logs its wall-clock elapsed | shipped in v1.5.1, `040d2ab` — `runner._active_clock` (monotonic) feeds one `active_elapsed()` helper used by the success, failure and Ctrl-C exits; `elapsed_seconds` is a field on `RunRecord`, not prose spliced into `detail`. Proved with a fake clock across all three exits; a real suspend is still not re-derivable and isn't claimed. `CHANGELOG.md` | 2026-08-02 |
| W-0.9.2-02 | a routine that spent its retry budget on failures reads green in `Last run` | shipped in v1.5.1, `31ae418` — `schedule.assess()` is the real logic and returns a compact state (`due`/`settled`/`not yet`/`command`/`disabled`/`invalid`/`unreadable`/`held`/`retry limit`); the hub's one cell became three fields, `Last run` (never coloured), `Result`, `Schedule`. `why_not_due()` is a one-line view over `.reason`, so every existing caller is unchanged. `CHANGELOG.md` | 2026-08-02 |
| W-0.9.1-07 | chat and routine stop sharing one chat session id | shipped in v1.5.1, `040d2ab`/`68d89fd` — `<routine-id>/<run-number>` on history, a completed `/routine`, the routines screen's generated help and `open`; the number is allocated inside `append_log`'s own append, legacy lines derive oldest-first, and the SQLite id stays an internal pointer with `db.routine_session()` still the provider check. Verified on a real log: 19 derived, run #20 persisted (`N-1.5.1-05`). `CHANGELOG.md` | 2026-08-02 |
| W-0.9.1-03 | internal name: cfc → Cooking for Cats, everywhere it's printed | shipped in v1.5.1, `9369233` — `ui.DISPLAY_NAME`, with an AST sweep (`tests/test_ui.py::test_display_name_sweep`) that derives every literal `cfc` left in top-level source and fails on anything outside a two-entry reasoned allowlist. Identifiers, paths, `[cfc]` budget notes and `[cfc direction]` deliberately unchanged. `CHANGELOG.md` | 2026-08-02 |
| W-0.9.1-04 | private-chat wording: "in memory" → what it means for your data | shipped in v1.5.1, `9369233` — the entry notice states the destruction boundary, that this is *local* privacy only and the provider still sees the messages, blocked model writes, `/export` as the one named exception, and `/database on` as read-only. Exercised in the playtest with nothing reported against it. `CHANGELOG.md` | 2026-08-02 |
| D-10 | a broken routine reads green on the hub's Routines panel | closed against the shipped code rather than by new work, as the v1.5.1 scope allowed. The middle tier is covered by `hub._routine_problem_count`/`_print_routine_problem_nudge` (v1.2), a *validation* signal deliberately separate from due-ness; the third tier by the same count including `bad`. v1.5.1 answered the entry's first open question without a new colour — `disabled`/`command`/`invalid` still share dim, but the cell now prints the word, so the conflation the entry names is resolved in text. Body moved whole to `legacy/BACKLOG.md` | 2026-08-02 |
| Q-1.5.1-03 | the coder's `git checkout -- hub.py` discarded uncommitted work — a build-handoff failure, a `CODER.md` failure, or a reason to raise the reasoning level? | answered — none of the three, and the dial is the wrong lever. Reasoning level buys depth on ambiguous problems; this was a tool-semantics slip (`checkout` restores from the last *commit*, not from the last edit), which more thinking does not prevent. The gap is now covered by `HANDOVER.md`'s testing habit: restore a deliberately disabled guard by re-editing it, or commit/stash first; never use `git checkout` to recover uncommitted work | 2026-08-02 |
| N-1.5.1-04 | the run log line still ends `(session 909)` after `W-0.9.1-07` | nothing owed, in scope by design. The v1.5.1 scope keeps the existing session id as an internal field and limits the run reference to the routines screen, `history`, a completed `/routine` and `open`. The log is the store, not a surface: `read_log` parses that field, `open` still needs it, and removing it would break `db.routine_session()`'s provider check. Recorded so it doesn't come back next playtest as a missed rename | 2026-08-02 |
| N-1.5.1-05 | the first real append after the update might renumber an existing log — raised during coding, not by use | nothing owed, checked on the live log rather than on a fixture. `test.md` holds 20 records: 19 legacy lines derived 1–19 oldest-first, and the first post-update run persisted `run #20` with `elapsed_seconds=49.0`. Legacy lines are byte-identical and keep their old `(37s)` prose inside `detail`. The one thing the coding handoff asked to be looked at once, looked at once | 2026-08-02 |
| N-1.5.1-06 | `W-0.9.1-08` and `W-1.1.1-01` sat in `Open` while already closed in v1.3 | corrected, not reported — removed from `Open`, where both were duplicates of their own closed rows. Flagged during planning as reconciliation and left to this pass. Same class as `N-03`/`N-04`/`N-05`: found by checking a row before relying on it | 2026-08-02 |
| D-15 | presets had never been sent to a real provider | closed after Cas sent an active preset through `deepseek-v4-pro:thinking` and confirmed the declared parameters were accepted; broader provider coverage is not claimed | 2026-08-01 |
| W-1.3-02 | `/delete chat` from the hub | shipped in `979370b` — hub `d` uses the shared delete resolver, confirmation and index-clean deletion | 2026-08-01 |
| W-1.3-03 | choose a chat's id yourself — collision warns, never replaces | shipped in `979370b` — hub `c` and `/new <id>` create only positive, unused ids | 2026-08-01 |
| W-1.4-03 | `/delete chat main`, beside `/delete chat <id>` | shipped in `979370b` — Main is resolved by identity and still requires the target confirmation | 2026-08-01 |
| W-1.3.1-05 | the chat start tips predate the commands and the context they'd now teach | shipped in `979370b` — the tips now teach the v1.5 chat controls | 2026-08-01 |
| B-1.5-02 | `preset_params` is documented as taking preset names; it takes parameter names | fixed in the v1.5 triage, `f37fe95` — `config.example.py` and `models.py`'s field table both said "the `PARAMETER_PRESETS` keys verified for this id". Following it writes `preset_params=["creative"]`, which `models.load()` rejects at import with nothing catching it, so cfc doesn't launch. Blocked the tag: the v1.5 scope gives `config.example.py` the job of teaching the record, and the presets had no writable way in. Confirmed live — Cas's own `config.py` has two presets and no declaration, so every model answered "no presets compatible", which is what "didn't properly test `/preset`" was. No test could have caught it; the file's *code* was always valid. `CHANGELOG.md` | 2026-08-01 |
| N-1.5-01 | `c` at the hub creates a chat instead of offering to rename one | nothing owed, premise wrong on both halves. `c` does exactly what its own generated help line says — *create a chat at a chosen id*, `W-1.3-03` as scoped — and the prompt says `New chat id (a positive number)` before anything is written. Renaming is not missing either: `/title <id> <new title>` renames **any** session by id from inside a chat (`main.py:1054`), Main excepted by identity. What is genuinely absent is a rename at the *hub*, which is `W-10` and is a want, not this. The `c` created at #900 is undone with `d` | 2026-08-01 |
| Q-04 | why do models turn a two-line explanation into obsessive drama-language? | answered, and half the premise is right. The register in `config.example.py` is real and it is a failure: it is written by a reader of the source for a reader of the source, so every clause defends a decision instead of telling you what to type. But it isn't a model tic without a cause — this repo's own documents ask for exactly it. `HANDOVER.md` is *supposed* to argue, `CHANGELOG.md` is *supposed* to say why it mattered, and `config.example.py` sits next to both and got written in their voice. The rule it should have been under is `HANDOVER.md`'s own *"say it once, then stop"* and standing decision 13's *the only shipped file that instructs a human*. Instructions and reasoning are different genres and the file has been carrying one in the other's clothes. `W-11` is the work | 2026-08-01 |
| B-07 | a provider error on the first turn left the chat permanently untitled | fixed in the v1.4.1 triage — v1.4.1's own regression, found by reading and confirmed against session 185 of the playtest. The gate widened from `turn_count == 1` to *the first ordinary chat turn that produced an answer*, `db.count_chat_answers` supplying the second half; both clauses verified by deleting the other. Did not block the tag: the entry claims the busy window and the no-retry rule, and both held. `tests/test_turn_paths.py`, `CHANGELOG.md` | 2026-08-01 |
| B-08 | `tests/test_turn_paths.py` wrote fabricated `title` errors into the live `~/.cfc/errors.log` | fixed in the v1.4.1 triage — `D-08` reopened one file over, four records already written and deleted by hand. `errorlog.LOG_PATH` now redirected with the same guard `test_model_revert.py` uses, verified by removing it. One older artefact left in place on purpose (a real 503 attributed to `stub-model`), named in `CHANGELOG.md` | 2026-08-01 |
| N-05 | `B-05` and `D-11` closed in v1.3 and were never moved out of `BUGS.md`/`BACKLOG.md` | corrected by handing it to the manager pass rather than by a debugger-session edit, since that pass is already moving four entries this loop. Same class as `N-03`/`N-04`: found by checking a row before relying on it. Neither file may hold a closed entry — `HANDOVER.md`, *Which file owns what* | 2026-08-01 |
| N-1.4.1-01 | model answers came back with `$\rightarrow$` instead of `→` | nothing owed, and the premise was wrong — the model wrote the LaTeX. All three messages in the database containing `rightarrow` are `google/gemma-4-31b-it`, one of them from 2026-07-30, two days before `9251de8`; the stored text is byte-for-byte what was on screen, and 320 other messages carry a real `→` and render fine. `emoji=False` only stops `:word:` substitution and was never in the path. Rendering LaTeX would be a new feature, not a repair | 2026-08-01 |
| Q-1.4.1-03 | how hard is it to change `[cfc]` to another marker shape later? | answered — cheap, with one caveat. `governor.DIRECTION_OPEN`/`CLOSE` are two constants with one producer, no parser anywhere, and a request-only payload, so a shape change is a two-line edit with no migration and no stored data. The caveat is that `agent.py`'s two budget notes hardcode `[cfc]` and share no constant with the governor — unify first, change once. `[:remember …]` is a different family: durable, parsed by `db._MARKER_RE`, and must not be swept along with it | 2026-08-01 |
| Q-1.4.1-04 | why is a routine/chat context line in `errors.log` called `error`? | answered — yes, it is that simple. Two record kinds, `launch` and `error`; the rest of the header is context (session, model, interrupted-turn count, phase) and the error itself is the indented line beneath it. `errorlog.py`'s header explains the split | 2026-08-01 |
| Q-1.4.1-05 | is `ui.CONNECTION_STYLE`'s comment a 3,375-character explanation of 297 characters of code? | answered — 3,482 characters of header comment over a table whose data is about 900 (the 2,295 figure includes two inline comments). Three claims in it are load-bearing; the rest is the story of how each was found, which `HANDOVER.md`'s decision 16 already tells at length. Cas's call: the answer is enough, no row opened | 2026-08-01 |
| B-1.3.1-02 | typing while cfc is silently busy after a turn puts a line you never typed into the chat | fixed in v1.4.1, `0cc952e` — one shared `_finish_turn` owns the context bar, a visible `finishing turn` marker, titling and auto-embed, and only then prints the blank line that used to come first. `tests/test_turn_paths.py`. Owed a move to `legacy/BUGS.md` | 2026-08-01 |
| B-06 | `ui.console` still rewrites `:word:` in chat content as an emoji | fixed in v1.4.1, `9251de8` — `Console(markup=False, emoji=False)`. `tests/test_ui.py`. Owed a move to `legacy/BUGS.md` | 2026-08-01 |
| D-13 | a failed title call is silent everywhere, and the next turn inherits the titling | fixed in v1.4.1, `0cc952e` — `api.generate_title` raises `TitleGenerationError` instead of returning the `(untitled)` sentinel; the finisher logs it with `where="title"` and prints one yellow line. The eligibility half needed a second pass, `B-07`. Owed a move to `legacy/BACKLOG.md` | 2026-08-01 |
| D-1.4-02 | the routines screen labels a routine by its slug id; the hub and `show` use its display name | fixed in v1.4.1, `a59d97e` — `screens._routine_row` and the validation lines beneath the table read `Routine.name`, both widths off the same row dict. `tests/test_screens.py`. Owed a move to `legacy/BACKLOG.md` | 2026-08-01 |
| W-1.4-05 | one document listing every way cfc injects into a chat | shipped in v1.4.1, `a6e8e38` — tracked `SYSTEM_INJECTIONS.md`, checked rather than authoritative: `tests/test_system_injections.py` derives every direct model-facing role producer from source and fails in both directions. Eleven anchors, two of them found by the derivation rather than pre-named | 2026-08-01 |
| B-1.4-01 | `/add <path>` in Main chat answers with the fixed-profile refusal and never attaches | fixed in the v1.4 triage, `c3650df` — the refusal guarded the whole verb from above the path branch; it now sits at the layer, so explicit kinds and resolved pool names refuse and everything else takes the ordinary path. The pool search still runs first for Main, so `/add relax.md` still means the trait. Blocked the tag: the work order's step 5 claims attachments stay available, and the test proving it used a tag — the one part of `/add` that was never behind the guard. `tests/test_mainchat_turns.py` now drives a real file attachment through Main. `CHANGELOG.md` | 2026-08-01 |
| N-1.4-07 | the hosted-embedder advice (`W-0.9.1-05`) could not be exercised in the playtest | nothing owed. The `hosted` state needs `probe()` to fail *and* `EMBED_BASE` to be non-local (`preflight.py:306`), which cannot arise on a machine embedding against localhost — killing LM Studio produces `not running`/`no server`, correctly. It is covered by behaviour rather than by wording in `tests/test_connection.py`. To see it once by hand: point `EMBED_BASE` at an unroutable non-local address (`http://192.0.2.1:1234/v1`, TEST-NET-1) and launch; the hub light goes red with the new advice, and putting the old value back is the whole undo | 2026-08-01 |
| Q-1.4-06 | how well do models read emoji, and what do they cost in tokens | answered during the v1.4 manager pass: common emoji are generally read as concepts; composed emoji and terminal/code-point handling are the practical risks, and kaomoji cost more because their punctuation is less frequently merged. No tokenizer measurement was available locally, so the token figures remain approximate | 2026-08-01 |
| N-04 | the stale open `W-0.9.1-06` row (fixed in v1.3, closed below, still listed as open) | corrected, not reported — removed from `Open`. Same class as `N-03`/`N-0.9.2-04`: found by checking a number before relying on it, this time inside the planning carry-forward, which is what let it reach the next handoff before this session caught it | 2026-08-01 |
| W-1.3.1-03 | a selected model sticks until cfc quits, instead of resetting to `MODEL` per chat | fixed in v1.4 — `main._process_model` is one process-wide selection, read at every `run_session` open instead of the session's own stored `model` column; `/model` is the only thing that changes it, carried through `/q`, `/new`, a private side trip in either direction, and a command-screen round trip. A fresh launch still starts at `MODEL`. `tests/test_process_model.py`. `CHANGELOG.md` | 2026-08-01 |
| W-1.3.1-01 | `/model` says nothing when the model it switches to can't use tools | fixed in v1.4 — prints a `Note:` through the same `tools_unsupported_reason()` seam the header and `/tools` already read, immediately after a successful switch, only when tools were otherwise about to run this session. `tests/test_model_tools_notice.py`. `CHANGELOG.md` | 2026-08-01 |
| W-1.3.1-04 | `/status`'s empty-Context row says "no messages yet" and never mentions First Message | closed, no code owed — already true on reading: both the session header and `/status` already print `empty — no messages yet`, and a First Message counts toward the Messages total (hub) and gets its own state row when a persona is attached. The open row predated a check against the actual source | 2026-08-01 |
| W-0.9.1-05 | "hosted embedder unreachable" says what's wrong, not what to do | fixed in v1.4 — the shared `ui.CONNECTION_STYLE['hosted']` text (rendered by the hub light, `/connect` and the config screen) and `preflight.ensure()`'s own launch-report line both say to check the connection, `EMBED_BASE`/`EMBED_KEY` and the provider's status, then retry `/connect embedding`. Stays the one red state — `ensure()` still returns early rather than reaching the local `lms` fixer, so decision 16's split is unchanged. `commands.connect_embedding()`'s "start LM Studio yourself" fallback is now gated on the failure actually being local. `tests/test_connection.py`. `CHANGELOG.md` | 2026-08-01 |
| Q-1.3.1-06 | options, risks and decisions for an on/off toggle on the trait cadence and the tone reader | answered — the trait half already has an off switch (`GOVERNOR_TRAIT_INTERVAL <= 0`) and `governor.ordinary_instruction` already takes `interval` as a parameter, so the seam exists. The tone half has none. Both off returns a byte-identical pre-1.3 envelope, which is what makes the toggle worth having as an experiment control. Three real decisions: where the state lives (config / session local / db column), that an off period *skips* trait refreshes rather than deferring them, and that the governor needs a `/status` row or the toggle becomes the silent state `1.3.1-01` is about | 2026-07-31 |
| N-03 | the stale open `D-11` row (fixed in v1.3, closed below, still listed as open) | corrected, not reported — removed from `Open`. Same class as `N-0.9.2-04`: found by checking a number before relying on it | 2026-07-31 |
| W-09 | a First Message has no visible state anywhere — `/status` never names it, and an unconfigured directory reads as a persona with no companion | fixed in v1.3.1 — one row, shown only when a persona is attached: `pools.first_message_status()` (shared with `load_first_message` via `_first_message_lookup`, so `/status` can't drift from what session-open actually does) names `ready`/`none for <persona>`/`not configured`/`unavailable — <reason>`. The ordinary no-persona case stays quiet — no fourth row. `tests/test_pools.py`, `tests/test_first_message.py`, `tests/golden.py`. `CHANGELOG.md` | 2026-07-31 |
| W-02 | `/export` and `/routine`'s listing commands are still hand-verified | fixed in v1.3.1 — `tests/test_export.py` now drives a representative exported document (attachment/tool-call/tool-result rendering, frontmatter totals, transcript order, title-safe re-export, an absent session) read back from a temp db/vault; `tests/test_routines.py` adds `show_routines()` coverage (empty store, and a mixed store: healthy, disabled, invalid-but-parseable, and unparseable). `do_routine` and `run_routine` were already covered and the stale claim otherwise — `HANDOVER.md`'s own "goes stale in the safe-looking direction" warning, one row over. `HANDOVER.md`/`README.md` corrected, `CHANGELOG.md` | 2026-07-31 |
| W-0.9.1-01 | `VAULT_PATH` → a name that says "chat export" | fixed in v1.3.1 — `CHAT_EXPORT_DIR` is the documented key and the single runtime name (`export.chat_export_dir()`); an existing `config.py` defining only `VAULT_PATH` still exports, the new key resolved first and never written back. `screens.py`'s two config renderings go through the same function, so export and `/config` can't disagree. `config.example.py`, `README.md`, `HANDOVER.md`, `tests/golden.py`'s fixture redirection, `tests/test_export.py`, `tests/test_private.py`. `CHANGELOG.md` | 2026-07-31 |
| N-1.3-01 | `FIRST_MESSAGES_DIR` missing from `config.py`, so First Message silently did nothing | nothing owed. Cas's own gitignored per-deployment file, which predates the key and which he repaired before this session; `config.example.py:44` shipped it correctly, so no user was served this. The code is behaving as documented — `pools.py:41-44` falls back to `""`, `first_messages_dir()` reads that as unset and returns `~/.cfc/first_messages`, which doesn't exist, so `load_first_message` returns `None` at `pools.py:366-367` and nothing prints. Same class as `N-01`. What it cost half a playtest is `W-09`, and that is the row with work in it | 2026-07-31 |
| B-05 | `run` from the routines screen uses a different model than `/routine` | fixed in v1.3 — `ScreenTable`/`build_table`/`enter` carry `chat_model`, `main.py:753` supplies the session's model and `screens.py:588` passes it to `do_routine`; a routine's own `model:` pin still wins. The screen entered with no chat behind it still falls to `default_routine_model()`, which is now correct by design rather than by omission. `BUGS.md` → `legacy/BUGS.md`, `CHANGELOG.md` | 2026-07-31 |
| D-11 | `/tools` puts `TOOLS_ROOTS`/`WRITE_ROOTS` in the golden baseline | fixed in v1.3 — the fixture points both at a real `tempfile.mkdtemp()` outside the checkout, guarded by `assert_not_repo_or_real_roots`, which refuses the source tree and refuses Cas's own configured roots and is verified by disabling it (`tests/test_golden_fixture.py`). Baseline re-recorded. `BACKLOG.md` → `legacy/BACKLOG.md`, `CHANGELOG.md` | 2026-07-31 |
| W-0.9.1-02 | "Msgs" → "Messages" in the picker | fixed in v1.3 — `hub.py:103`, widened 4→8 so the header fits, and the count includes a First Message so it agrees with `/status` and the export's `total_messages` | 2026-07-31 |
| W-0.9.1-06 | a line when a routine starts, and that Ctrl-C interrupts it | fixed in v1.3, and the line was the smaller half: `run_routine` now catches `KeyboardInterrupt` itself (previously uncaught, and it would have taken the REPL down) and logs `cancelled` as a third status beside `ok`/`failed`, keeping the transcript as it stood. `last_settled` skips cancellations for due-ness and `last_success` tightened to `"ok"` only, so a cancelled run no longer silently absorbs a weekly slot. `CHANGELOG.md` | 2026-07-31 |
| W-0.9.1-08 | `/connect embed` accepted too | fixed in v1.3 — `main.py:1171` | 2026-07-31 |
| W-1.1.1-01 | `/connect <typo>` says "connect target" and "Targets:" | fixed in v1.3 — `main.py:1174` says "Unknown connection" and "Available connections" | 2026-07-31 |
| W-1.2.1-02 | `/config` doesn't say `help` exists on the way in | fixed in v1.3, and widened to all three screens rather than config alone — `render()` is the function all three share, the wiki and routine screens have the identical gap, and scoping it to config would have meant a conditional inside a shared function to preserve the bug on two of three readers. Debugger agreed. `screens.py:676`/`709` | 2026-07-31 |
| N-02 | `run_routine`'s Ctrl-C handling can't be proven on the `--run-due` path | nothing owed — there is no human on that path to press it, so the event the test would simulate does not occur there. The `except` clause is exercised from a stub in `tests/test_routines.py` and by a real Ctrl-C on the on-command path; an OS signal into an unattended process is a different event and this code claims nothing about it. Raised during coding rather than by use | 2026-07-31 |
| Q-1.3-04 | what breaks if the model list sorts alphabetically? | answered — very little, and not where it looks. `/list models` and `/model <n>` both read `models.listed_ids()`, so sorting inside that one function keeps display and numbering in step by construction; `resolve_model` counts candidates rather than taking the first, so name matching is order-blind. Three costs: `<n>` stops being stable across config edits, alphabetical over `vendor/model` sorts by vendor (`glm` files under `z`) so it wants the bare name, and `MODELS` itself must not be sorted — `known_ids()` documents listed-first and `_from_legacy`'s routine default is `ROUTINE_MODELS[0]`, which `models.py:92-93` explicitly warns must not become "whichever sorts first" | 2026-07-31 |
| Q-1.3-05 | why ~5% as many 503s on streaming as on non-streaming? | answered — tool calling is not involved. All 42 in `~/.cfc/errors.log` carry one body, *"Managed edge assertion configuration is missing"*, the provider fault of `N-0.9.2-01`/`Q-1.1.1-03`. 37 tool-path against 5 streaming (~12%, not 5%), nine of the 37 at the routine ceiling and so not chat turns at all. The asymmetry is exposure: a streaming turn issues one request, a tool turn up to 25, any of which ends the turn. `call 0` dominance is arithmetic — a 503 ends the turn, so failures pile at low indices under any rate. 25 of the 42 are from the playtest day itself | 2026-07-31 |
| Q-0.9.2-01 | how long is the 0.9 → 1.0 absence window? as written it can be waited on but never failed | answered by decision, not by measurement — no window length gets set. `B-01` stays open on the bug list, `watching`, and does not gate any tag; it closes the way it always could, by recurring and being explained, or it simply outlives the releases around it. The rule that was forcing a length onto it was the loop, not the entry. `HANDOVER.md`'s open-threads paragraph on `B-01` updated to match | 2026-07-31 |
| Q-1.1-12 | one model-capability table in `config.py` instead of four lists | built and shipped in v1.2.1 — `models.py`'s one ordered `MODELS` list replaces `MODELS`/`TOOLS_MODELS`/`ROUTINE_MODELS`/`MODEL_LIMITS`, validated at import, both load-bearing distinctions the private-roadmap answer called out kept (`ROUTINE_MODELS`'s per-model judgement, `TOOLS_MODELS`'s verified-not-assumed provenance). `CHANGELOG.md`, 2026-07-31 | 2026-07-31 |
| W-1.2-03 | "not in TOOLS_MODELS" names a config list, not what to do about it | fixed in v1.2.1 — `commands.tools_unsupported_reason` replaces it at all four sites (session header, `/status`, `/tools`, the one-time `/tools on` notice), naming the tool-capable models instead of the internal list. `CHANGELOG.md` | 2026-07-31 |
| W-1.2-02 | `/model <all digits>` refuses without offering a way through | fixed in v1.2.1 — an out-of-range `/model <n>` now explains that digits pick a row off `/list models` rather than set a raw id. `CHANGELOG.md` | 2026-07-31 |
| B-1.2-04 | a model revert lands on a model the provider already rejected | fixed in v1.2.1 — `run_session` keeps a `rejected_models` set, fed on HTTP 400 only; `revert_bad_model` disarms rather than switching onto one already in it. `tests/test_model_revert.py`, 32 assertions, both turn paths. `BUGS.md` → `legacy/BUGS.md`, `CHANGELOG.md` | 2026-07-31 |
| B-04 | the connection advice says "in a chat" on a screen that can do it | fixed in v1.2.1 — every fixable `ui.CONNECTION_STYLE` row names both `/connect embedding in a chat` and `connect embedding in config`, one string, driven through all three real renderers. `BUGS.md` → `legacy/BUGS.md`, `CHANGELOG.md` | 2026-07-31 |
| N-1.2.1-01 | routines fire for thinking models, and the warning is gone | nothing owed, and the premise is wrong on both halves. The warning is intact — `commands.do_routine` still refuses to run unvetted, reworded not removed (`git diff v1.2..HEAD`), and `models.is_routine_vetted` returns False for both of Cas's thinking ids. And the run never touched a thinking model: session 170 is stored with `model = deepseek/deepseek-v4-pro-cheaper`. The warning correctly did not fire because the model was vetted. The v1.2.1 model-config change is exonerated; what made the model change is `B-05` | 2026-07-31 |
| Q-1.2.1-03 | can `q` after `/wiki` or `/routine` return to the previous chat? | answered — cheap for a normal chat, structurally impossible for a private one. `_enter_screens` (`main.py:491`) ends the session with the same cleanup `/q` does, and `screens.enter` already returns a session id for the routine-transcript case, so a normal chat has an id to reopen and `repl()` already knows how. A private chat has none: it lives in `db(":memory:")` (decision 10), the screen is handed `app_conn` instead, and ending the session destroys the database. So it would work for one chat and silently not for the other, which is decision 15's exception clause. Building it means saying that out loud, not plumbing it. **Cas's call: a private chat not being continuable is the design** — so the private branch is a stated refusal, not an unbuilt half | 2026-07-31 |
| B-1.2-01 | the wiki screen prints `/wiki diff [all]` and then refuses it | fixed in the v1.2 triage, `eafbbdd` — commands.py's three wiki entry points take a `lead`, defaulting to the chat form so a forgetful screen caller reproduces a visible refusal. The `all` in the same lines became `vault`, decision 13's own rule. Blocked the tag: v1.2's wiki bullet claims generous phrasing *because* strictness makes a working feature read as broken, and the screen was printing the strictness itself. `CHANGELOG.md` | 2026-07-31 |
| N-1.2-02 | `/model 0394103958` is refused as a bad list number, not set as an id | nothing owed on the behaviour. `h_model` takes `raw.isdigit()` as the numbered-selection path before any name resolution, on purpose: `/list models` numbers its rows and `W-1.1-10` made that the fast path. The cost is that a model id made only of digits is unreachable through `/model`, which Cas raised and dismissed in the same breath. The wording half is `W-1.2-02` | 2026-07-31 |
| D-1.1-05 | `TRANSIENT_STATUS_CODES` omits 504 | fixed in the v1.1.1 triage — 504 joins 429/502/503 in the retryable set. 408 left out on purpose: resending a request the client itself timed out proves nothing. `CHANGELOG.md` | 2026-07-30 |
| W-1.1-03 | auto-revert fires on a transient status, not just a rejection | fixed in v1.1.1 — `handle_turn_error` checks `api.is_transient_status` before reverting a just-switched model; a transient status leaves the switch standing, only a real rejection or an untyped error reverts it. `CHANGELOG.md` | 2026-07-30 |
| W-1.1-04 | the hub lists 5 routines of 7, and cuts the never-run ones first | fixed in v1.1.1 — `hub.HUB_ROUTINES` raised so the panel lists all seven; still a bounded display cap, not derived from the vault. `CHANGELOG.md` | 2026-07-30 |
| D-1.1-08 | `/clear notes` names no paths and its prompt reads as a list row | fixed in v1.1.1 — the preview names the notes-inbox and archive paths, and the confirmation is no longer indented into the filename list. `CHANGELOG.md` | 2026-07-30 |
| D-1.1-09 | retired `:` commands survive in ~25 source comments | fixed in v1.1.1 — swept from source comments and four test docstrings; `agent.py:509`'s restated invariant cut to three lines and a pointer to standing decision 2. The two runtime strings the sweep didn't reach are `B-03`. `CHANGELOG.md` | 2026-07-30 |
| W-1.1-10 | `/model <n>` — the lists display numbers, the picker accepts them | fixed in v1.1.1 — `/list models` numbers its rows in displayed order, `/model <n>` switches straight to that id with no second picker, an out-of-range number leaves the model unchanged | 2026-07-30 |
| D-12 | three files still describe the pre-v1.0 auto-revert scope | fixed in v1.1.1, alongside `W-1.1-03` — the remaining stale claim, `tests/test_model_revert.py`'s docstring, corrected in the same edit | 2026-07-30 |
| B-03 | two runtime strings still print retired `:` syntax at a user | fixed in the v1.1.1 triage commit — the private-chat banner's `:export` and `_session_arg`'s `f":{cmd.verb} <session id>"` fallback, both flagged by the coder as outside `D-1.1-09`'s comments-and-docstrings scope rather than absorbed. Found by reading, so a plain-sequence id. Same class as `B-0.9.1-02`; never an open `BUGS.md` entry, reasoning in `CHANGELOG.md` | 2026-07-30 |
| Q-1.1.1-02 | what are the consequences of streaming with tools on? | answered — there is no such turn. `main.py:931` picks one path per turn and tools win: tools on *replaces* streaming with `agent.agent_turn`, it doesn't add tools to a streamed call. Costs the token-by-token paint and moves the read timeout from a 300s per-chunk gap to a 600s total silence. If the model isn't in `TOOLS_MODELS` the toggle is inert and you stream — `/tools` already prints all three switches, so that state is diagnosable | 2026-07-30 |
| Q-1.1.1-03 | are the 503s burst/rate limits? | no. All 17 in `~/.cfc/errors.log` carry one body — *Managed edge assertion configuration is missing*, the `N-0.9.2-01` provider fault reproduced from outside cfc. Zero 429s ever logged, and nano-gpt's limits return 429. Density is wrong too: 17 across three days, never more than 2 in a minute, against a 10-per-10s bucket. Blind spot stated: private chats log nothing | 2026-07-30 |
| D-09 | `reflection`'s read roots don't cover its prompt — vault edit, no code | Cas's call — the routine's read/write roots now cover what its prompt reads. Vault-side, no code | 2026-07-30 |
| Q-02 | the tag-blocking test reads an entry the new shape writes afterwards | settled — the private roadmap's version proposal is the finish line a finding is tested against, named as such in `HANDOVER.md`; it's already finite and written before testing, `ROADMAP.md`'s stub isn't. The debugger tested v1.1 against `ROADMAP_PRIVATE.md` without the rule saying so; it now does | 2026-07-30 |
| W-1.1-11 | `/outbox` is a real alias and the README never says so | fixed directly, manager pass — one line added where `/list <kind>` is documented. Doc-only, no code | 2026-07-30 |
| W-0.9.1-09 | `/status` is too dim to read | fixed in `9ac48d6` — "Last turn" no longer renders in the same dim grey as an inactive setting | 2026-07-30 |
| W-05 | `/file` and `/move` take a title, not a number | fixed in `9ac48d6`/`53a7f1e` — `/file <title>` and `/move`, both shipped as scoped. Split into `W-05a`/`W-05b` at the backlog; both closed, `legacy/BACKLOG.md` | 2026-07-30 |
| D-02 | processed notes stay in `00 inbox/notes` forever — `/clear notes` | fixed in `9ac48d6` — manual archive into a dated batch folder, moves rather than deletes. The UX gap it left (no paths shown, prompt reads as a list row) is `D-1.1-08`. `legacy/BACKLOG.md` | 2026-07-30 |
| W-1.1-07 | `/file <title>` matched nothing across five attempts | fixed in `53a7f1e` — the matcher was never the defect. `/list outbox` printed filename, title and corpus tag on one line with nothing saying where the title ended, and `/file` takes the whole remainder; every attempt pasted the visible line back with the tag attached. The tag now leads, so the title runs to end-of-line. Pinned as a round trip (render the label, slice past the dash, assert `match_title` finds it) rather than against the literal label, which passed throughout the failure | 2026-07-30 |
| N-1.1-01 | a declined tool call was followed by a 503 | nothing owed, and the report's premise was backwards — the denial line printed, so the denial ran and was recorded; the 503 hit the *next* API call. Standing decision 2 held: every call in that message was answered before the exception, so the session was not poisoned. The outage is `N-0.9.2-01` | 2026-07-30 |
| N-1.1-06 | a 400 after a missing comma in `config.py`, with no fallback | nothing owed on either half. Python concatenated two adjacent string literals into one `MODELS` entry, so `config.py` asserted a model that doesn't exist and cfc believed it. Routines have never had a fallback and structurally cannot: `revert_model` is a `run_session` local, and a routine has no previous model to return to. The 400 correctly failed the run and named the id, which is how the typo was found. The generalisation is `W-08` | 2026-07-30 |
| Q-01 | does `chat.db` want an off-machine copy? snapshots share its disk, exports are 1.5MB of 28 | settled for v1.1 — cfc remains local-only. Off-machine durability is optional user-managed backup: make an integrity-checked snapshot with `backup.py`, then copy it to a trusted private remote or other backup. Never copy the live database and do not make cfc push; a future DB-layer change is not part of this decision | 2026-07-30 |
| D-05 | six instruction files shared five sections word for word, unchecked | post-1.0 doc rewrite, step 6 — the shared half moved to `HANDOVER.md`, which every session reads anyway, so there is no pointer chain and no seventh copy. The six files became six specialist files carrying only what makes each session different; `CLAUDE.md` is auto-loaded and holds the human context and the loop table. Nothing is duplicated, so nothing needs checking | 2026-07-29 |
| W-06 | the roadmap becomes a feature description after 1.0 | post-1.0 doc rewrite, step 3 — `ROADMAP.md` is the front office now: two or three sentences, **Added**, **Fixed** at one patch-note line per fix carrying its id, Cas's note as the signature. v0.1–v1.0 stay in the old shape behind a boundary line. v1.1 stubbed | 2026-07-29 |
| D-0.9.2-01 | a transient provider status kills an unattended run | fixed post-v1.0 by Codex (`8b83d97`), routine-only and by status code alone — 429/502/503, matched on `status_code` rather than on wording, sharing `EMPTY_COMPLETION_RETRIES`' budget. All three of the entry's deferred questions answered as it framed them. Body in `legacy/BACKLOG.md` | 2026-07-29 |
| W-03 | document the skeleton, not just the app | v1.0 step 8 — the two filesystems and what each erases, the vault straddling both on purpose, why the embedder is a separate endpoint, and what backs up what. Verified against the real machine rather than written from memory, which is what turned up `Q-01`. `README.md` has the layout, `HANDOVER.md` the failure modes | 2026-07-29 |
| D-01 | golden baseline pins the live vault outbox | v1.0 step 7 — the outbox is redirected to a fixture in `capture()`, Cas's call between the entry's two options, so `/outbox` stays in the characterization sweep. One filable proposal and one refusal, so both renderings stay pinned. Fixing it surfaced `D-11`, the same bug one line over. Body in `legacy/BACKLOG.md` | 2026-07-29 |
| W-01 | zero recall hits and "nothing worth reporting" are identical | v1.0 step 6 — closed without code, as the version scoped it. The chat half stopped being true in v0.9: three outcomes, driven and confirmed on screen. The routine half is unbuildable — **no routine can reach recall**, the four tools are `list_dir`/`read_file`/`grep`/`write_file` and `commands.py` is the only importer of `search` or `recall`, so there is no call site the policy could live at. Successor opened in `ROADMAP_BEYOND.md`, number unclaimed | 2026-07-29 |
| D-0.9.1-03 | `/routine new` checks the trigger at save, and drops the creation | v1.0 step 4 — all four holes: `trigger` and `on_failure` re-prompt against the *same* functions `validate()` calls, a taken id is caught at the name, and a cancelled model picker no longer saves the routine you were abandoning (that one wrote a file). Every exit says it is one, which the entry called the half that actually bit. Body in `legacy/BACKLOG.md` | 2026-07-29 |
| B-0.9.1-01 | tool denial renders as `error: user denied` | v1.0 step 3 — fixed at the render, payload untouched: `← read_file denied at the prompt` in dim, dim red kept for errors that are errors. The producer/parser pair it would have created was **closed** by sharing `commands.DENIED`/`SKIPPED` rather than added to `HANDOVER.md`'s table — the graph allowed the import. Body in `legacy/BUGS.md` | 2026-07-29 |
| B-0.9.1-03 | the light's advice names a command the hub won't take | v1.0 step 2 — the advice names its own context (*in a chat*), so one string stays true in all three renderings; the table was not forked. The hub-accepts-the-command option from the report was declined on scope, and the reason is in the row's body. Body in `legacy/BUGS.md` | 2026-07-29 |
| D-0.9.1-01 | two connection states share a colour; the pairing looks swapped | v1.0 step 2, same decision — the dot now carries recoverability, not severity: orange where `/connect embedding` will try, red where it isn't cfc's to fix. `hosted` and `not running` swapped, which is what the report saw. No colour invented. Body in `legacy/BACKLOG.md` | 2026-07-29 |
| B-02 | `README.md` still said LM Studio can't be started from WSL | fixed in v1.0 step 2 — found reading the file the light is documented in. It is the claim `HANDOVER.md` keeps in *rejected designs* as its one deliberately-preserved mistake, disproven by Cas from a cold machine on 2026-07-27; the README and a `ui.py` comment never caught up. A correction of something factually wrong, not a restyle | 2026-07-29 |
| D-06 | the release order exists only in gitignored files | v1.0 step 1 — `CLAUDE.example.md` carries it as five steps with the tag last, and the tag-blocking test. Two homes now, not seven: the six are governed by *all six or none*. Body in `MANAGER CLAUDE.md`'s hazard list | 2026-07-29 |
| D-07 | `CLAUDE.example.md` still describes the old single-file arrangement | v1.0 step 1, same edit — it describes the split as a pattern with its costs, not six files reproduced, and names no model or reasoning level so the post-1.0 workflow change is a clean cut rather than a correction | 2026-07-29 |
| N-0.9.2-04 | the shared-section count in all six `* CLAUDE.md` files said four and five were identical | corrected, not a finding anyone reported — the omitted section is *Six kinds of session*, the one the paragraph is inside, so *"this one included"* had never parsed. Found by checking the number before shipping it into a tracked file. Doesn't close `D-05`; it is the first evidence in it | 2026-07-29 |
| Q-0.9.1-01 | can `~/.cfc/errors.log` be gitignored before it exists? | nothing owed — it lives in `$HOME`, outside the working tree, so git has never been able to see it, and it isn't in `TOOLS_ROOTS` either | 2026-07-28 |
| Q-0.9.1-02 | is nano-gpt's inline moderation involved in the 400? | no — `api.py` sends `model`/`messages`/`stream`(/`tools`) and nothing else. Also rules out two of the three documented causes: cfc never sends `max_tokens` or `stop`. Recorded in `BUGS.md`'s entry | 2026-07-28 |
| Q-0.9.1-03 | "new chats index themselves" — is that claim wrong? | the claim was true and the reading was the defect: chats *are* embedded every turn (`source='chat'`), and `recall.py` filters to `provider='wiki'`, so they are indexed into something `/recall` doesn't read. README reworded | 2026-07-28 |
| N-0.9.1-01 | local paths in `BUGS.md`, `BACKLOG.md`, `commands.py`, `ui.py`, the golden baseline | nothing owed, Cas's call — the name/path pair isn't identifying on its own. **Not to be written about outside a gitignored file**, which is why this row is here and nowhere else | 2026-07-28 |
| N-0.9.1-02 | `.gitignore` was carrying changelog-shaped prose about the CLAUDE split | reworded the same day, one line | 2026-07-28 |
| Q-0.9.1-04 | has a routine ever run on a real scheduled tick? | yes, and read off the run logs rather than the report: `short term memory` (`0300`) ran unattended at 05:45 on 28-07, `medium term memory` (`weekly 0330`) at 03:30 on 27-07. Closes a `HANDOVER.md` open thread and one of v1.0's three unproven paths | 2026-07-28 |
| B-0.9.1-04 | routine freshness applies a daily rule to every trigger kind | fixed in v0.9.2 — the column renders `schedule.why_not_due()` instead of hours-since-last-run; 5 of 6 live rows were untrue, not 1. Body in `legacy/BUGS.md` | 2026-07-28 |
| B-0.9.1-02 | `config.example.py` documents twelve retired `:` commands | fixed in v0.9.2 — swept both config files; every command validated through `parse.parse`, none written as an alias. The entry's four-aliases split was five: `models` → `list models`. Body in `legacy/BUGS.md` | 2026-07-28 |
| D-0.9.1-02 | config comments carry the whole origin story | fixed in v0.9.2, same pass — *first clause is a date or a version* found three history paragraphs; the load-bearing ones stayed. Body in `legacy/BACKLOG.md` | 2026-07-28 |
| D-08 | the test suite writes to the live `~/.cfc/errors.log` | fixed in v0.9.2 — guards in `test_model_revert.py`, `test_routines.py` and `golden.py`, each verified by breaking it; `test_agent.py` checked and clear. 32 fabricated entries deleted by hand, not the 4 the entry claimed. Body in `legacy/BACKLOG.md` | 2026-07-28 |
| D-03 | Obsidian `{{ }}` collides with cfc placeholders | done in the vault, Cas's post-tag report — cfc owed nothing and `PLACEHOLDERS` is unchanged. Body in `legacy/BACKLOG.md` | 2026-07-28 |
| N-01 | `DATABASE_ACTIVE` is in `config.example.py` and not in `config.py` | nothing owed — `main.py` imports it under `try/except ImportError` with a `False` default, which is the pattern for an optional key. Found drafting v0.9.2; the two files' key *order* has drifted, so aligning it while both are open is free and optional | 2026-07-28 |
| N-0.9.2-01 | every routine returning HTTP 503 | nothing owed — a nano-gpt outage (`managed_mode_misconfigured`), reproduced from outside cfc with a bare one-message payload and no tools, and recovered by 09:52. Not every routine either: `test` succeeded at 09:47:46 between two failures, `short-term-memory` at 08:47:58. The cfc-owned half is `D-0.9.2-01` | 2026-07-29 |
| N-0.9.2-02 | the outage cost `short-term-memory` its whole day, not just three runs | nothing owed on the cap — `MAX_RETRIES_PER_DAY = 3` is written for exactly this ("a routine that fails for a permanent reason would otherwise run every quarter of an hour until midnight, at full API cost, unattended"). It cannot tell a permanent failure from a transient one, and it should not have to: that belongs one level down, in `D-0.9.2-01` | 2026-07-29 |
| N-0.9.2-03 | a routine ran for 2h49m (`10148s`) against a 600s read timeout | nothing owed — the machine suspended at 03:30:10 mid-request and resumed at 06:19:18 (no scheduler ticks fired in between, which is what proves it). httpx's read clock is the guest's and was paused with it, so the timeout was never late — it never ran. Decision 12 held: the date was computed at 03:30 and injected, so the resumed run was still working on the right day. The misleading elapsed figure is `W-0.9.2-01` | 2026-07-29 |
| W-04 | the public-repo decision, and licensing if yes | decided, Cas's post-tag report: AGPL-3.0 is committed and the repo goes public with the v1.0 tag. `ROADMAP_PRIVATE.md`'s paragraph is now a record of a decision made, not a question — it moves to `ROADMAP.md` when v1.0's entry is written | 2026-07-28 |
