# Dev-time only. Nothing in the shipped app imports these.
#
# pillow — dev/bake_splash.py, which converts an image into an assets/*.raw
# splash asset. The runtime loads that asset with stdlib alone, which is the
# reason the format exists; keep Pillow out of requirements.txt so a clean
# checkout proves it.
pillow>=11.0

# pytest — runs the tests/ suite.
pytest

# pytest-asyncio — Textual's Pilot-driven tests (tests/test_cfc_tui.py) need
# a real running event loop per test, not the bare asyncio.run() wrapper the
# rest of Stage 3/4's async tests use directly.
pytest-asyncio>=1.4,<2.0
